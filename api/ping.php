<?php
// Публичный health-check: подтверждает, что API и БД доступны, БЕЗ авторизации
// и без каких-либо данных проекта. Раньше для этого использовался api/report,
// пока он был полностью публичным; после того как report.php стал требовать
// requireAuth() для блока 'pulse' (2026-07-13, коммит a77ca21), анонимный пинг
// оттуда стал получать 401 на КАЖДОЙ загрузке — index.html считал сервер
// недоступным (db=null), что также отключало реальную запись в saveState()
// (см. `if (!db) return` — запись уходила только в localStorage) и обновление
// state при заходе (loadState() требует db && authToken). Разделяем: report —
// бизнес-данные, требует авторизацию; ping — просто "жив ли сервер".
require_once __DIR__ . '/storage.php';
apiHeaders('GET', '*');
header('Cache-Control: no-store');
pdo()->query('SELECT 1'); // подтверждаем реальную доступность БД, не только PHP
echo json_encode(['ok' => true]);
