# Линии розлива → компактный список (как реакторы) + показ всех линий

Проблемы из скрина: (1) блок в старом стиле, надо «так же» как реакторы;
(2) показывались 2 линии из 3 — фильтр `.filter(x => x.parts > 0)` скрывал линию без партий.

## Сделано
- [x] Убрал фильтр `parts>0` → показываются ВСЕ `state.pouringLines` (простаивающие приглушены)
- [x] Переписал блок в табличный вид `.dash-ll-row` + переиспользование `.dash-rl-*` (единый стиль с реакторами)
- [x] Колонки: Линия · Выпуск · Партий · ≈ шт/парт · Доля выпуска (полоска + %), сортировка по выпуску
- [x] Убрал верхний stacked-бар и мёртвый CSS `.dash-stack`/`.dash-pl*`
- [x] Синтаксис (vm, 0 ошибок) + харнесс: 3 линии (одна с 0 партий — показана как idle)
- [ ] Показать diff, ждать «go»

## Примечание
- По умолчанию линий 3: `['Линия 1','Линия 2','Ручной розлив']`. Если у пользователя третья
  переименована/добавлена — она тоже покажется (рендерим из `state.pouringLines`).
  Если линий реально 2 — значит третья не заведена в Настройках → добавить там.
