# Реакторы → Вариант A: компактный список-таблица вместо 7 карточек

Выбор пользователя: Вариант A (детальный компактный список).
Колонки: Реактор · Статус · Продукт · Варок · Выпуск · Загрузка (с полоской).
День = live-статусы (Варит/Розлив/Ожидание/Простой) + время варки в тултип.
Неделя/Месяц = агрегат (Работал/Простой, топ-SKU, варок/выпуск/загрузка за окно).

## Задачи
- [x] CSS: заменил `.dash-reactors`/`.rc2*` на `.dash-rl*` (список-таблица)
- [x] Убрал `.rc2` из shadow-правила light-темы
- [x] Разметка: обернул `#dash-reactor-cards` в `.dash-card` + `.dash-rl-scroll`
- [x] Переписал `renderReactorCards(range)` → шапка + строки; статус-пилл, полоска загрузки
- [x] День: время варки → title (тултип); Неделя/Месяц: загрузка относительно самого загруженного
- [x] Синтаксис (vm, 0 ошибок) + харнесс день/неделя/месяц + idle
- [x] Обновить DASHBOARD_PROGRESS.md
- [ ] Показать diff, ждать «go» для push

## Review — 2026-07-05
- Вариант A из согласованного визуала. День сохраняет прежнюю live-логику (тот же расчёт статусов).
- Удалён мёртвый код: `.rc2*`, `.dash-reactors`, `const DASH_IDLE_HOURS`.
- Проверка — только изолированный Node-харнесс (memory: no writes against live DB).
